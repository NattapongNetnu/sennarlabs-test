#Front-end Developer Sennalabs

The Project is created by ReactJS. You can runs the app in the development mode with below command.

## `cd sennalabs`

## `yarn install`

## `yarn start`

Open [http://localhost:3000](http://localhost:3000) to view it in the browser.